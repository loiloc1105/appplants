class CartItem {
    constructor(quantity, sum, productTitle, productImage,productPrice) {
            this.quantity = quantity,
            this.sum = sum,
            this.productTitle = productTitle,
            this.productImage = productImage,
            this.productPrice=productPrice
    }
}
export default CartItem